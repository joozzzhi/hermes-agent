# Aida — общая память

Гермес, телеграм-бот и сессии разработки пользуются одной памятью: одна таблица в Postgres
(Supabase), к которой подключаются все. Что сказано в Телеграме в июле, здесь доступно без
пересказа — и по решению оператора это **его собственное прошлое**, а не цитата из чужого
разговора: Гермес говорит о нём от первого лица.

## Что уже работает

**Вспоминает.** Перед каждым непустым обращением он ищет в общей памяти и подкладывает
найденное в разговор, а над ответом видно, что память была поднята и сколько записей нашлось.

**Запоминает.** Осознанная просьба «запомни» кладёт мысль в ту же базу — и её тут же видит
бот. Заметки, которые Гермес ведёт о человеке сам, зеркалятся туда же с высшим приоритетом:
их писали руками, а не подслушали. Снятая заметка перестаёт всплывать, но из базы не
исчезает — ошибочное снятие стоит одного запроса, а не факта.

**Копит разговор.** Каждая законченная реплика ложится в локальную очередь рядом с остальным
состоянием агента. Ответ при этом ничего не ждёт: до базы реплики довозит ночная работа.

Пишет только тот агент, с которым человек разговаривает. Помощник в фоне, задание по
расписанию или разбор старого разговора в общую память не пишут — иначе она наполнится
черновой работой, которой никто не произносил.

**Разбирает ночью.** Раз в сутки очередь уезжает в общую базу дословно, из привезённых обменов
дешёвая модель достаёт то, что стоит помнить долго, и заодно проставляются векторы записям,
которые до сих пор находились только по совпадению слов.

Обмен уходит из очереди разбора, только когда модель на него ответила: не ответила — подождёт
следующей ночи. Реплики к тому времени уже в базе, и разобрать их иначе было бы нечем.

Ставится в расписание командой `hermes cron`, скрипт лежит в папке профиля
(`scripts/aida_nightly.py`, копия — рядом с плагином). Отчёт после каждого прогона короткий:
сколько реплик довезено, сколько фактов найдено, сколько записей получили смысловой поиск.

## Про модель для разбора

Первым идёт Gemini: у него своя квота, и разбор не отнимает её у ответов человеку. Запасных
моделей по умолчанию нет намеренно — бесплатные варианты, на которые рассчитывал бот, закрылись
(проверено 21.09.2026: все три отвечают 404 и предлагают платную версию). Подставить платную
молча значит начать тратить чужие деньги. Кому нужен запасной вариант — перечисляет модели сам
в `AIDA_DISTILL_MODELS`.

## Настройка

```
hermes memory setup      # выбрать aida и вставить строку подключения
```

Нужны:

- `SUPABASE_DB_URL` — строка подключения к базе (Supabase → Project Settings → Database → URI).
- `OPENROUTER_API_KEY` — необязателен. С ним память ищется ещё и по смыслу, без него — только
  по совпадению слов.

Включается строкой `memory.provider: aida` в `config.yaml`; выключается снятием той же строки.

## Как устроен поиск

Порт поиска телеграм-бота, вплоть до констант: полнотекстовый поиск по русской морфологии,
слова вопроса взвешены по редкости (частое слово сигнала не несёт), отсечка по силе лучшего
совпадения, плюс поиск по смыслу через pgvector, и два списка сливаются по RRF.

Две базы искали бы одно и то же по-разному, и записи одной стороны стали бы недостижимы для
другой — поэтому SQL здесь намеренно повторяет чужой, а не улучшает его.

## Длинная мысль

В колонке помещается две тысячи знаков, а реплика бывает длиннее. Длинная запись не режется, а
делится на пронумерованные части: оригинал не уничтожается никогда — пересказ вместо него это
то, откуда берутся выдуманные подробности.

## Когда база недоступна

Разговор продолжается без памяти: ответ не ждёт и не падает, индикатор честно показывает, что
вспомнить не удалось, и следующая минута проходит без новых попыток достучаться. Запись, которая
не прошла, не выдаётся за сохранённую — агенту так и говорят, что записать не удалось. Реплики
разговора при этом копятся в локальной очереди и никуда не деваются.

## Как убрать за собой

Каждая строка, написанная отсюда, помечена в поле источника. Всё, что когда-либо записал этот
плагин, снимается одним условием — записи бота и сессий разработки при этом не затрагиваются.
